"""`media-dedup review`: burst series one at a time, in the browser, with the keyboard.

A small local web server (pure asyncio) shows each series, saves every decision at once
in a decisions file, and never touches a photo: `clean --decisions` does, journaled.
"""
