# TODO execution plan

> Generated by `/todo-plan` on 2026-09-26 — **do not edit by hand**, this file is regenerated from
> `.todos/*.md` on every run. Priority and batching live in the TODO files themselves.
>
> Open TODOs: 12 — Critical: 0 · High: 1 · Medium: 5 · Low: 6 · Locked: 0
> Batches marked `~` were inferred from `**Files**`, not declared.
> Lots require a shared path, never a shared theme — each lot names the directory its members
> share. A standalone TODO is the normal case: run it in its own session, `/clear` between.

## Recommended order

First, the only High TODO: **0020** (standalone, maintainer steps — publish 0.1.1). Then:

| # | Lot | Priority | TODOs | Prompt |
|---|-----|----------|-------|--------|
| 1 | scan | Medium | 3 | `/todo 0007 0006 0018` |
| 2 | docker | Medium | 2 | `/todo 0010 0021` |

The Medium standalone TODOs below (0019, 0017, 0004) rank with these lots: run each in its own
session.

## Lots

### 1. scan — Medium

**Shared context:** `src/media_dedup/scan/` — all three; `src/media_dedup/scan/filters.py`
(0007, 0018).

**Why grouped:** 0007 teaches the scan filters about sidecars; 0018 decides whether those same
filters open up to any file type; 0006 extends the image check of the same package to RAW files.
One load of `scan/` serves all three.

**Prompt:** `/todo 0007 0006 0018`

| Order | ID | Priority | Title | Depends |
|-------|----|----------|-------|---------|
| 1 | 0007 | Medium | Clean orphan sidecar files (.xmp, .aae, .thm) | — |
| 2 | 0006 | Low | Support RAW files (previews and integrity) | — |
| 3 | 0018 | Low | Evaluate a generic mode: duplicates of any file type | — |

### 2. docker — Medium

**Shared context:** the repository root: `Dockerfile` (0010), `pyproject.toml` and `uv.lock`
(0021).

**Why grouped:** both shrink the shipped image — 0010 replaces the 141 MB static ffprobe, 0021
weighs dropping numpy (70 MB). One `build` + `dive` measurement serves both. 0020 (same batch)
is kept apart: High priority, and maintainer steps on Docker Hub and GitHub.

**Prompt:** `/todo 0010 0021`

| Order | ID | Priority | Title | Depends |
|-------|----|----------|-------|---------|
| 1 | 0010 | Medium | Replace the static ffprobe (141 MB) with a demux-only build | — |
| 2 | 0021 | Low | Evaluate dropping numpy (70 MB of the image for three array operations) | — |

## Standalone

One `/todo NNN` each, in its own session — highest priority first. These share no files with a
sibling (or declare none yet), so there is nothing for a lot to save.

| ID | Priority | Title | Batch | Why not lotted |
|----|----------|-------|-------|----------------|
| 0020 | High | Publish version 0.1.1 on Docker Hub (maintainer steps) | docker | priority diverges from lot 2; maintainer accounts needed |
| 0019 | Medium | Explain unwritable mount points instead of crashing | mounts | only member of its batch |
| 0017 | Medium | Decide per folder pair in the report, then `clean --decisions` | review-ui | no path shared with 0004 (TBD); its dependency 0014 is done |
| 0004 | Medium | Build a keyboard-driven review UI for series | review-ui | no `**Files**` yet (TBD); its dependency 0003 is done |
| 0005 | Low | Identify similar videos (re-encoded copies) | similarity | only open member of its batch; its dependency 0001 is done |
| 0009 | Low | Evaluate Immich as the long-term family photo library | decision | decision-only, no `**Files**` yet |
| 0011 | Low | Translate the few built-in CLI strings (Usage, Options, --help) | i18n | only member of its batch |
